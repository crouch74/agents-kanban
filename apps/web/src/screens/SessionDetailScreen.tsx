import type { ReactNode } from "react";

type SessionDetailScreenProps = {
  children: ReactNode;
};

export function SessionDetailScreen({ children }: SessionDetailScreenProps) {
  return children;
}
