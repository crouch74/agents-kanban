import type { ReactNode } from "react";

type ProjectOverviewScreenProps = {
  children: ReactNode;
};

export function ProjectOverviewScreen({ children }: ProjectOverviewScreenProps) {
  return children;
}
