import type { ReactNode } from "react";

type WorktreeInventoryScreenProps = {
  children: ReactNode;
};

export function WorktreeInventoryScreen({ children }: WorktreeInventoryScreenProps) {
  return children;
}
