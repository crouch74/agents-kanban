[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "acp-api"
version = "0.1.0"
description = "FastAPI entry point for Agent Control Plane."
requires-python = ">=3.12"

[tool.setuptools]
packages = ["app"]

